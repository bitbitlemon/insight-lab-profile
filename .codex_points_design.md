# insight-lab-profile 统一积分模型 v4 设计评审备忘录

## 0. 评审结论摘要

我建议 v4 采纳“单一总分池 + 金额类强压缩 + 开发团队分配 + 易灌水来源 cap”的总体方向。现有 v3 的核心失衡点非常明确：`industrial_points` 是 1 元 = 1 分无封顶，`product_stage` 的 `launch/operating` 也把金额 1:1 加入，导致金额类来源在数学上直接吞没论文、项目申报、专利软著、公共贡献等所有非金额来源。只靠季度工资换算系数 `C_q ∈ [0.9, 1.1]` 无法修正这种结构性爆炸。

Claude 提案中的 `amount_points = K * ln(1 + 元 / A)`，取 `A=10000, K=42`，整体合理。它把 10 万元横向项目压到约 100 分，能和一篇中科院一区论文、国家级项目立项同量级，符合负责人已拍板的“开发 ≈ 科研”政策。

但 v4 不能只改曲线。还必须同时处理四个问题：

1. 金额口径：按合同额、到账额、净收入还是可支配贡献额计分。
2. 去重口径：同一笔收入不能在 `industrial` 和 `product_stage` 重复吃完整金额分。
3. 团队口径：开发金额分必须按项目团队分池，不能继续单人 100% 入账。
4. 时间口径：科研产出周期长，开发收入和产品阶段更容易按季度集中入账，需要结算层做滚动归属或平滑。

## 1. 对提案 1：金额类对数压缩曲线

### 1.1 K/A 取值评价

`A=10000, K=42` 是可接受的第一版参数。它的实际含义是：

- `A=10000`：1 万元是金额贡献开始显著计分的基准尺度。
- `K=42`：把 10 万元锚定在约 100 分。
- 曲线边际递减很强：从 10 万到 100 万，金额增加 10 倍，但积分只从约 101 增到约 194。

这个锚点站得住。原因是 v3 中科研高价值锚点已经有两个稳定参照：

- 中科院一区论文：100 分总池。
- 国家级项目立项：100 分。

若负责人政策是“一个有分量的横向项目 ≈ 一篇顶刊量级”，则 10 万元横向项目锚定 100 分是合理的。低于 10 万的横向项目通常不应压过顶刊，高于 50 万或 100 万的横向项目应体现规模差异，但不能按线性规模碾压科研。

我建议把参数写成锚点公式，而不是硬编码神秘常数：

```text
amount_points = 100 * ln(1 + amount_yuan / 10000) / ln(11)
```

这等价于 `A=10000, K≈41.70`，比 `K=42` 更清楚：10 万元严格等于 100 分。实现时可以保留四舍五入展示。

### 1.2 我建议的金额曲线

推荐 v4 使用以下基础曲线：

```text
raw_amount_points = 100 * ln(1 + recognized_amount_yuan / 10000) / ln(11)
```

其中 `recognized_amount_yuan` 不建议无脑使用合同总金额，应优先采用以下口径：

1. 已到账金额：适合工资结算，防止签约未回款提前兑现。
2. 可支配贡献额：如果合同包含大量外协、硬件采购、转付成本，应按扣除直接外部成本后的金额计。
3. 分期确认：大额合同按到账或验收节点分期入账，但同一项目的累计金额进入同一条曲线，不能拆成多条小合同重复吃低金额高边际。

推荐对照表：

| 金额 | 推荐 v4 分数 |
|---:|---:|
| 5,000 | 16.9 |
| 10,000 | 28.9 |
| 20,000 | 45.8 |
| 50,000 | 74.7 |
| 100,000 | 100.0 |
| 200,000 | 127.0 |
| 500,000 | 164.0 |
| 1,000,000 | 192.5 |
| 5,000,000 | 259.3 |

这个表和 Claude 的表基本一致，只是把 10 万精确锚定为 100 分。我的判断是：不需要改成幂函数或分段阶梯。对数曲线更适合这里，因为它连续、可解释、可校准，也能自然处理从几千元到几百万元的跨度。

### 1.3 是否需要封顶

不建议对单个大额产业项目做硬封顶，例如 200 分或 300 分封死。硬封顶会让 100 万和 500 万项目差异过小，削弱真实产业突破的激励。

但建议加两类“软约束”：

- 单项目金额分超过 200 分：进入双审，核查金额口径、成本扣除、团队分配、是否拆单。
- 单成员单季度来自金额类的到账确认分超过 150 分：不禁止入账，但超出部分可延后到后续季度结算展示，工资结算按滚动窗口确认。

这样保留大项目差异，又避免单季度排行榜和工资被一次性到账击穿。

### 1.4 `product_stage` 金额加成

`product_stage` 的阶段固定分 `init=5, mvp=10, internal_qa=15, launch=30, operating=60` 可以保留。但 `launch/operating` 的金额加成必须和 `industrial` 共用同一条金额曲线，并且要防重复：

- 如果 `industrial` 已按同一笔合同或收入记过金额分，`product_stage` 只记阶段固定分。
- 如果 `product_stage` 是产品收入唯一入口，则金额分进入产品团队分池。
- 同一产品的多次 `operating` 入账应按累计金额计算增量分，而不是每次独立套一次 `ln(1+x/A)`。

增量公式：

```text
delta_points = f(cumulative_amount_after) - f(cumulative_amount_before)
```

这是防拆单的关键。

## 2. 对提案 2：开发团队贡献分配

### 2.1 必须从单人入账改为团队分池

提案 2 必须采纳。开发类项目继续只挂 `member_open_id` 会造成两个问题：

- 项目负责人吃满分，实际研发、交付、商务、运维贡献被隐藏。
- 为了拿分，系统会激励大家把团队工作包装成个人成果。

v4 应把 `industrial` 和 `product_stage` 都改成“事件总池 + 成员权重分配”，和论文、竞赛同一个台账模型思路一致。

### 2.2 开发角色权重建议

开发团队不应直接照搬论文 author weight。论文作者顺序通常代表学术贡献排序；开发项目的贡献结构更像项目制，角色之间不是线性作者顺序，而是负责人、核心交付、关键商务、参与支持等不同责任。

建议默认角色权重：

| 角色 | 默认权重 | 说明 |
|---|---:|---|
| owner / 项目负责人 | 1.00 | 对目标、交付、验收和团队协调负责 |
| tech_lead / 技术负责人 | 0.90 | 架构、关键实现、质量兜底 |
| core / 核心贡献者 | 0.70 | 承担关键模块或主要交付 |
| business_core / 商务核心 | 0.70 | 关键客户、合同、回款、需求闭环 |
| contributor / 参与者 | 0.35 | 明确任务贡献 |
| support / 支持者 | 0.15 | 测试、文档、运营、临时支持 |

分配方式：

```text
member_points = project_pool_points * member_weight / sum(all_member_weights)
```

同时设置默认保护规则：

- 单人最高分配比例默认不超过 50%。
- 两人项目负责人最高可到 60%。
- 单人超过 60%、任一成员 0%、或实际分配偏离默认权重超过 20 个百分点时，触发双审。
- 金额分和阶段固定分可以使用同一团队分配，也可以允许阶段固定分偏交付团队、金额分偏商务+交付团队，但必须在快照中记录分配理由。

### 2.3 与论文权重的关系

开发权重不应与论文 author weight 完全一致，但应保持“同一思想”：

- 都是总池分配，不是每人复制一份总分。
- 都要记录角色、权重、最终比例。
- 都要有异常比例双审。

论文权重可保留现有 `first=1.0, second=0.6, third=0.3, other=0.15, corresponding_bonus=0.2`。开发权重应独立建表，因为开发项目中“负责人”和“核心贡献者”的关系不等同于“一作”和“二作”。

## 3. 对提案 3：防灌水与 cap

### 3.1 易灌水来源

除软著外，以下来源也容易被批量化或拆分刷分：

| 来源 | 风险 | 建议 |
|---|---|---|
| `ip.software_copyright` | 软著可批量申请，质量差异大 | 年度个人有效上限 24 分，即最多 3 件按 8 分计；超过只展示不结算或进入复核 |
| `ip.design_granted` / `utility_granted` | 外观、实用新型相对易批量 | 年度个人上限 36 分；同一产品族或同一技术主题合并计算 |
| `grant.applied` | 申报动作可重复堆叠 | 季度个人上限 12 分，年度上限 24 分；同一项目多渠道申报只取最高 |
| `training.general_training` | 低成本刷课 | 月度个人上限 4 分，季度上限 8 分 |
| `training.industry_cert` | 证书质量差异大 | 年度个人上限 30 分；高价值证书单独白名单 |
| `contribution.participant` | 参会、参与易堆量 | 保留现有月度 cap，建议 public 总季度上限继续生效 |
| `contribution.reflection` | 低质量反思可批量 | 保留月度条数限制，并增加审核抽样 |
| `product_stage.init/mvp/internal_qa` | 可拆多个产品或重复报阶段 | 同一产品同一阶段只能记一次；相似产品线需合并审查 |
| `product_stage.operating` | 可按月重复确认运营 | 金额按累计增量曲线计，运营固定分每产品每自然年最多一次 |
| `industrial` | 拆合同、关联交易、虚高硬件成本 | 按项目累计金额曲线；超过阈值双审；关联交易和转付成本扣除 |
| `adjust` | 管理员调整口径不透明 | 所有正向 `adjust` 必须有原因、审批人和上限；建议季度个人不超过 30 分，例外需备注 |

### 3.2 cap 设计原则

cap 不应只按 source_type 粗暴封顶，应分三层：

1. 低质量高频来源：硬 cap。软著、一般培训、participant、reflection 适合硬 cap。
2. 中价值但可重复来源：合并 + cap。同主题专利、同项目申报、同产品阶段适合合并后计分。
3. 高价值低频来源：不硬 cap，但双审。一区论文、国家级项目、大额横向不建议硬 cap。

推荐个人 cap：

| 类别 | 建议 cap |
|---|---|
| 公共贡献 `contribution` | 沿用季度个人 40 分，可评估是否把 participant 月 cap 10 降到 6 |
| 软著 | 年度个人结算上限 24 分 |
| 实用新型 + 外观 | 年度个人合计上限 36 分 |
| grant applied | 季度个人 12 分，年度个人 24 分 |
| general training | 月度个人 4 分，季度个人 8 分 |
| industry/academic cert | 年度个人 30 分，白名单高价值证书可豁免 |
| product fixed stages | 同一产品同一阶段一次；同一成员季度来自固定阶段分建议不超过 45 分 |

## 4. 风险与遗漏

### 4.1 科研仍可能被边缘化

即使金额类对数压缩，科研仍有被边缘化的风险：

- 开发产出更容易按季度入账，科研论文和项目周期更长。
- 开发团队可以把阶段、收入、培训、公共贡献叠加在同一季度，科研成果更稀疏。
- 金额类有更强的可见性和管理偏好，容易在评审中被认为“更实”。
- 论文总池 100 分分给多人后，个人通常只有 20-60 分；而 10 万横向按团队分配后，如果团队较小，负责人可能仍拿到 40-60 分，季度上更占优。

建议补充两个机制：

1. 科研里程碑积分：投稿、返修、录用、发表可以拆阶段，但总额必须受最终论文池约束，避免只在发表季度才体现。
2. 滚动周期评价：排行榜可显示季度榜，但工资或核心评价建议引入近 4 个季度滚动分，降低科研周期劣势。

### 4.2 统一指标失真的其他隐患

单池总分会把不同价值类型强行相加，容易出现以下失真：

- 金额不等于技术难度：一个低技术含量但金额较高的交付可能压过高创新但暂未变现的科研。
- 合同额不等于利润或实验室贡献：硬件采购、外包转付、客户垫资会虚高金额。
- 产品阶段分和产业金额分边界不清：同一项目可能先报 launch，再报 industrial，再报 operating。
- 公共贡献和职务分可能成为稳定刷分来源：尤其 duty 默认月度入账，如果管理岗位较多，会改变科研/开发排名。
- 负分力度偏弱：数据造假 -50 分低于一区论文 100 分，也低于 10 万横向 100 分，可能不足以覆盖严重违规收益。

建议：

- 金额分按“已到账且扣除直接外部成本后的认可金额”。
- 同一业务对象建立唯一 `source_group_id` 或等价快照字段，防重复入账。
- 严重违规扣分应可按已获收益倍数追回，例如撤销对应成果积分并额外扣 50-100 分。

### 4.3 时间维度是否需要归一

需要。单池总分不是问题，问题是不同来源的产出周期不同。

推荐结算层采用双视图：

- 排行展示：季度分 + 年度分 + 近 4 季度滚动分。
- 工资结算：季度分为主，但大额金额类和长期科研成果可做归属平滑。

大额金额类建议：

- 按到账或验收节点入账。
- 单季度金额分进入个人结算超过 150 分时，超出部分滚动到后续季度，展示总分仍可一次确认。

科研建议：

- 论文可设置里程碑，但最终论文录用/发表时进行冲销或补差，保证总池不超过论文等级分。
- 国家级/省级项目可区分申报、答辩、立项、验收，但总额应有上限。

### 4.4 `C_q ∈ [0.9, 1.1]` 是否够用

作为工资预算稳定器，`C_q` 的 0.9-1.1 只能微调，不能承担结构平衡责任。v3 金额线性爆炸时，`C_q` 完全不够；v4 对数压缩后，`C_q` 仍只适合作为预算平滑器。

建议：

- 保留 `C_q ∈ [0.9, 1.1]`，不要扩大到更大范围。扩大只会让成员难以理解每分价值。
- 结构平衡应靠源头规则解决：金额曲线、团队分池、cap、时间归属。
- 每季度输出科研/开发/公共占比报表。如果连续两个季度开发类总分超过全池 70%，或科研类低于 20%，规则委员会应复盘参数，但不要用 `C_q` 临时扭曲个人分数。

## 5. 对提案 4：category 维度

提案 4 应采纳。`category` 应继续用于分析展示，而不是结算分池。

但当前 v3 的 `CATEGORY_BY_SOURCE` 把 `paper`、`grant`、`ip`、`competition`、`duty`、`product_stage` 都归为 `business`，语义不够支持“科研/开发/公共占比”。建议 v4 至少补充一个分析维度，不一定替换现有 `category`：

```text
domain = research | development | public | penalty | admin_adjust
```

推荐映射：

| source_type | domain |
|---|---|
| paper | research |
| grant | research，horizontal grant 例外走 development/industrial |
| ip | research 或 development，按成果归属选择 |
| competition | research 或 development，按比赛类型选择 |
| industrial | development |
| product_stage | development |
| contribution | public |
| duty | public 或 admin_service，不建议混入科研/开发 |
| penalty | penalty |
| adjust | admin_adjust |

结算仍按单池总分，但看板必须能展示每个人、每团队、每季度的来源结构。否则单池会掩盖失衡。

## 6. 结构性建议：v4 整体方案

我对 v4 没有反对性的结构意见，但建议把它从“改几个函数”升级为“统一分池协议”。

推荐 v4 总体模型：

```text
source_event
  -> source_pool_points
  -> allocation_members(weights/shares)
  -> caps/decay/time_smoothing
  -> PointsLedger rows
```

每个来源都回答同一组问题：

1. 这个事件的总池是多少分？
2. 总池是否由等级、金额、阶段、人工审核共同决定？
3. 如果有多人，成员权重如何产生？
4. 是否有 cap、递减、双审阈值？
5. 分数归属哪个季度，是否需要滚动平滑？
6. 是否与其他来源存在同一业务对象，如何去重？

### 6.1 推荐 v4 最小可落地规则

1. 金额曲线统一为：

```text
f(amount) = 100 * ln(1 + amount / 10000) / ln(11)
```

2. `industrial` 使用团队总池：

```text
pool = f(认可金额)
member_points = pool * role_weight / sum(role_weights)
```

3. `product_stage` 使用：

```text
pool = stage_base + f(累计认可金额_after) - f(累计认可金额_before)
```

其中金额部分必须和 `industrial` 去重。

4. 开发角色权重独立于论文作者权重。

5. 软著、低价值培训、participant、grant applied、产品固定阶段设置 cap。

6. 大额金额类不硬封顶，但超过阈值双审和结算平滑。

7. 看板保留单池总榜，同时强制展示 research/development/public 占比。

8. `C_q` 只做预算稳定，不做结构修正。

### 6.2 最需要补的制度口径

v4 在实现前需要负责人确认以下制度口径，否则规则会在执行时被反复解释：

- 横向项目按合同额、到账额、净收入还是可支配贡献额计分。
- 产品收入和产业横向是否共用唯一金额池。
- 大额合同分期入账时，排行榜展示分和工资结算分是否允许不同。
- 开发团队分配由谁提交、谁审核、成员是否需要确认。
- 同一成果跨科研和开发属性时，是否允许拆分 domain。
- 历史 v3 已入账金额类是否重算，还是 v4 仅对新账生效。

## 7. 最终建议

v4 应采纳 Claude 的主方向，并把金额曲线写成“10 万元 = 100 分”的锚点公式。我的推荐版本是：

```text
amount_points = 100 * ln(1 + recognized_amount_yuan / 10000) / ln(11)
```

这条曲线能把开发金额压回科研同量级，同时保留大项目的边际差异。真正决定 v4 成败的不是 `K=42` 还是 `K=41.7`，而是金额口径、累计增量、防拆单、团队分池、时间平滑和 cap 是否一起落地。

如果只能做一个最小 v4，我建议优先级如下：

1. 立刻废止所有 1 元 = 1 分金额入账。
2. `industrial` 和 `product_stage` 金额部分统一使用对数曲线。
3. 金额类改团队分池，禁止单人默认 100%。
4. 同一项目累计金额按增量计分，防拆单。
5. 软著、培训、participant、grant applied 加 cap。
6. 看板展示科研/开发/公共占比，但工资仍按单一总分池。
