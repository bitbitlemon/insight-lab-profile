## 三阶段完善路线评审

P1 先补管理工具层是合理的，因为这些接口已经闭环但缺少前端入口，投入产出比最高。
积分录入属于高频后台操作，放在 AdminConsole 里与现有积分审核、申诉处理同域，优先级明确。
成员管理同样是权限敏感的基础资料维护，先提供列表、过滤、编辑、新建、删除可以支撑后续关系治理。
P2 的师生指导、审计同步、会议纪要更偏治理和追踪，依赖成员主数据稳定，排在 P1 后更稳。
P3 的任务、NavBar、看板扩展属于体验和入口收尾，不应阻塞管理闭环。
成员管理短期放 AdminConsole tab 更好，权限、入口、视觉和现有审批工具一致，开发成本低。
但长期如果成员字段继续扩展、涉及批量导入、权限矩阵、组织树，独立页 /admin/members 会更清晰。
建议当前先做 tab，后续把成员管理抽成组件，迁移到独立路由时成本可控。
主要风险是手动积分录入会直接影响 ledger，需要前端明确预估分、负分颜色和成功后的 ledger id。
扣分 custom_amount 的语义需与后端保持一致，前端只做范围提示，不自行推断复杂规则。
成员删除风险高，必须二次确认，并在 UI 上避免误触。
分页过滤要注意 status 参数当前并非后端查询条件，前端需本地过滤或后续补接口能力。
MemberPicker 有成员缓存，新建/编辑后可能不会立刻反映到 picker，需要后续考虑缓存失效能力。
遗漏点包括审计日志入口、成员字段校验规则、删除后关联数据影响提示和批量管理能力。
本次 P1 可接受先覆盖最小可用管理闭环，把强约束交给后端校验。

## P1 完成情况

已新建 frontend/src/api/pointsEntry.ts，封装 4 类积分提交和 3 类积分预估接口。
已更新 frontend/src/api/members.ts，补齐成员新建、更新 payload 类型和删除接口。
已更新 frontend/src/pages/AdminConsolePage.tsx，新增「积分录入」tab，支持项目申报、扣分、产业积分、产品阶段四类表单。
积分录入已接入 MemberPicker、发生日期、300ms debounce 预估、负分红色展示、成功 Toast 和提交后刷新。
已新增「成员管理」tab，支持关键词/角色/状态/部门过滤、分页列表、编辑弹层、新建弹层和删除二次确认。
构建验证：2026-05-22 执行 `cd frontend && npm run build` 通过；Vite 仅提示 vendor chunk 体积警告。
