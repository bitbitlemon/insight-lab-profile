# insight-lab-profile v4 Phase B 完成说明

## 改动文件

- `backend/app/routers/industrial.py`
  - 将产业积分录入从单个 `member_open_id` 改为 `members` 团队列表。
  - 返回 `ledger_ids`、`total_points`、`allocations`。
  - 同一事件使用同一个 `source_id` 写入多条 ledger。

- `backend/app/routers/product_stages.py`
  - 将产品阶段录入从单个 `member_open_id` 改为 `members` 团队列表。
  - 返回团队分配结果。
  - 产品阶段固定分和金额加成合并为总池后按开发角色权重分配。

- `backend/app/services/points_service.py`
  - `write_industrial_ledger` / `write_product_stage_ledger` 改为接收团队成员。
  - 使用 `allocate_dev_points` 按角色权重分配总池。
  - 每个成员写一条 ledger，保留同一个 `source_id`，写入成员占比、角色和金额/阶段说明。

- `backend/tests/test_smoke.py`
  - 更新产业积分 smoke 用例为团队版请求。
  - 断言多条 ledger、同源事件、总分池和成员分配。

- `frontend/src/api/pointsEntry.ts`
  - 增加 `DevRole`、`DevTeamMember`、团队版请求/响应类型。
  - 更新产业积分和产品阶段提交 API 返回团队分配结果。

- `frontend/src/pages/AdminConsolePage.tsx`
  - 在积分录入 tab 中新增开发团队编辑器。
  - 产业积分 / 产品阶段使用团队成员 + 开发角色录入。
  - 项目申报 / 扣分保持单人归属人不变。
  - 提交成功提示改为团队总分和人数。

## 验证结果

- 后端：`cd backend && .venv/bin/python -m pytest -q`
  - 结果：通过，`15 passed`

- 前端：`cd frontend && npm run build`
  - 结果：通过
  - 备注：Vite 输出了既有 chunk size warning，无编译错误。

## 取舍

- `total_points` 返回该事件的总分池，即每条团队 ledger 的 `base_points`，避免成员分配四舍五入后出现合计误差。
- 前端产业积分预估使用同一金额对数曲线公式做即时展示，最终入账仍以后端返回为准。
