# insight-lab-profile P2 完成记录

## 前端变更

- 新增 `frontend/src/api/advising.ts`：师生指导 CRUD 与分页列表客户端。
- 新增 `frontend/src/api/audit.ts`：审计日志分页、删除撤销客户端；兼容 `/audit` 与当前 `/audit_log` 路由。
- 新增 `frontend/src/api/sync.ts`：同步状态查询与全量同步触发客户端。
- 更新 `frontend/src/api/meeting_notes.ts`：补充详情查询与删除方法。
- 更新 `frontend/src/api/pointsEntry.ts`：新增 `previewIndustrialPoints({ amount_yuan })`。
- 更新 `frontend/src/pages/AdminConsolePage.tsx`：
  - 新增「师生指导」tab，支持分页列表、新建、编辑、删除二次确认。
  - 新增「审计日志」tab，支持动作与目标表过滤，删除类日志可撤销。
  - 新增「数据同步」tab，展示同步状态，支持全量同步二次确认与 loading。
  - 产业积分表单接入后端 300ms debounce 实时预估。
- 新增 `frontend/src/pages/MeetingNotesPage.tsx`：会议纪要独立列表页，支持详情 Popup 与有权限删除。
- 更新 `frontend/src/routes.tsx`：新增 `/meeting-notes` 路由。
- 更新 `frontend/src/pages/BoardPage.tsx`：新增「会议纪要」入口。

## 验证

- `cd frontend && npm run build`：通过。
